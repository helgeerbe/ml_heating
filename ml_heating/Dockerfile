# STUB FILE FOR HOME ASSISTANT DISCOVERY
# This is a minimal Dockerfile stub required for Home Assistant add-on discovery.
# The actual Dockerfile is located in ../shared/Dockerfile and gets copied here
# during the CI/CD build process, overwriting this stub.

ARG BUILD_FROM
FROM $BUILD_FROM

# Placeholder - real build instructions come from ../shared/Dockerfile
LABEL \
  io.hass.name="ML Heating Control" \
  io.hass.description="Physics-based ML heating control - Stable Channel" \
  io.hass.type="addon"

# This file will be completely replaced during build
CMD ["/bin/true"]
